- each friend in friends
	div.user( class=friend.id	)
//		div.userImage
//			img(src=#{friend.})
		div.colourPreview
			//canvas(id=friend.id, width='100%', height='100%')
			div.previewName
				p= friend.name
